// saved_location.dart
import 'package:flutter/material.dart';

class SavedLocationsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Saved Locations'),
      ),
      body: Center(
        child: Text('Saved Locations Page'),
      ),
    );
  }
}
